check_svc() {
SVC=$1
ESTATUS=$(systemctl is-enabled $SVC)
ASTATUS=$(systemctl is-active $SVC)
if [ "$ESTATUS" = "enabled" ]; then
	echo "$SVC is $ESTATUS."
	echo "$SVC is $ASTATUS."

else 
	echo "$ESTATUS"
fi
}

